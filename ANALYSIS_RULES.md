# 定量结论与一般设计规则 —— CNN ×「算子 / 拉格朗日设计空间」

来源: `operator-lab` 实测(真实训练 LeNet 式 CNN, MNIST)。
实验脚本: `backend/exp_rules.py`(结果: `data/exp_rules.json`, 页面 ④ 直接引用)。

## 实验设置

| 项 | 值 |
|---|---|
| 网络 | Conv8×5²-ReLU-Pool₂ → Conv16×5²-ReLU-Pool₂ → FC120→10 |
| 训练 | MNIST 15000×2 epochs · Adam lr 2e-3 · batch 64 |
| 算子扫描 | Conv1: 8 个单通道 2D 算子; Conv2: 16 输出×8 通道 = 128 个 2D 分量算子 |
| 约束注入 | 每步把 c1w/c2w 按 180° 对称性投影: `even: k←(k+k̄)/2`, `odd: k←(k−k̄)/2` |

对称性语义(按对话框架): **偶核 ⇔ 自伴 ⇔ 二次拉格朗日泛函可导出(L2)**;
**奇核 ⇔ 反自伴 ⇔ 陀螺/方向项**(能量守恒, 破时间反演); **正定符号 ⇔ L3 凸能量**。

---

## 实验 A — 对称约束的精度代价(同一 seed=7, 内部可比)

| 约束 | test acc | Δ vs 自由 | Conv1 奇部% | Conv1 偶部% | Conv2 奇部% | Conv1 PSD | Conv1 方向型 |
|---|---|---|---|---|---|---|---|
| 自由 | 0.9728 | — | 52.4% | 47.6% | 48.3% | 0 | 5/8 |
| **偶核(自伴/拉氏量可导出)** | **0.9734** | **+0.06 pp** | 0% | 100% | 0% | 0 | 5/8 |
| 奇核(反自伴/陀螺) | 0.9721 | −0.07 pp | 100% | 0% | 100% | 0 | 8/8 |

**核心定量结论 Q1(反直觉但实测成立): 奇偶结构是“自由自由度”。**
把两层卷积核全部投影为偶核(自伴 ⇒ 可导出二次拉氏量)后, 测试精度不但不降, 反而
**+0.06 pp**(0.9728→0.9734); 投影为纯奇核也只 −0.07 pp。原因: 手写笔画是**条带
(bar)结构** —— 中心对称的**各向异性偶核**(条形/二阶型, even 组仍有 5/8 方向型
滤波器)足以表征; 奇部只额外提供“一阶阶跃对比”, 对数字身份信息冗余。

> 推论: 想让卷积层落入拉格朗日/变分框架(偶核 = 自伴 = 可导出 δS/δu=K*u)是
> **可以免费得到**的结构约束 —— 物理一致、可解释的“势能型”CNN 层不需要牺牲
> MNIST 级精度。这是把本对话理论落到工程的关键数字。

**核心定量结论 Q2: 特征核 PSD ≡ 0(全部 7 个模型 × 136 个 2D 算子)。**
无论自由、偶核还是奇核约束, 训练后的卷积核符号**必然变号**(复符号或不定),
从不进入 L3 正定锥 ⇒ 特征层本质是**过零/差分测量器**, 不是能量泛函。
想要“能量/去噪/正则”语义必须**显式参数化正定核族**(对称非负符号), 不能靠训练自动获得。

## 实验 B — 多种子复现性(自由训练, 3 seeds)

| 量 | 均值 | 3-seed 范围 |
|---|---|---|
| test acc | 0.9707 | 0.9622 – 0.9766(抖动 ~1.4 pp) |
| Conv1 奇部能量占比 | 47.9% | 40.7% – 59.7%(漂移大) |
| Conv2 分量奇部占比(128) | 48.4% | 47.5% – 49.1%(±0.8%, 极稳定) |
| Conv1 方向型滤波器 | 5.7/8 | 5–6/8(总是多数) |
| Conv1 朝向均匀性 R | 0.42 | 0.12 – 0.66(不一定均匀) |
| PSD 计数 | 0 | 恒 0 |

**Q3: 奇偶预算分层规律。** 首层 Conv1 奇部占比随种子大幅漂移(41–60%), 深层
Conv2 的 128 个分量却稳定收敛在 48%±1 —— 说明网络对“哪个滤波器奇/偶”不敏感,
但对“深层整体奇偶配比”有自组织倾向(≈均分, 恰对应 Q1 的“自由”结论)。

**Q4: 方向多样性不是自动保证的。** 方向型滤波器稳定占多数(5–6/8), 但朝向覆盖
均匀度 R(0=全向,1=单向)在 0.12–0.66 间波动 —— 短训练/小数据下可能有方向盲区,
应当用 15° 方向直方图体检, 而非默认网络自发铺满所有朝向。

---

## 一般设计规则(由定量结果提炼)

1. **R1 — 先验约束“免费与否”用 A/B 投影协议实测, 不要猜。**
   同 seed/数据/轮数下, 每步把权重投影到目标子空间(偶核、奇核、对称轴、PSD…)
   与自由训练对比。本项目实例: 偶核约束 = **免费**(+0.06 pp)且顺带得到拉氏量可
   导出性; 奇核约束亦 ≈ 免费。对任何新任务(自然图像含阶跃边缘/纹理时, 奇部预期
   更重要)先跑该协议再决定是否强制约束。

2. **R2 — 若目标是“物理一致/变分/可解释”网络: 直接用偶核族, 然后按需显式加正谱。**
   - 偶核 ⇒ 每层是自伴算子、可写二次拉氏量 `S[u]=½∫u(K*u)`(或与激活匹配的广义势)。
   - 加符号约束(对称+非负谱)⇒ 凸能量/稳定层(L3)。此时滤波器应从**参数化的对称
     正定族**(高斯/协方差/格林核)出发或训练后做谱投影, 而不是期望自由 SGD 产出 PSD。
   - 实测表明偶核约束对条带/笔画类任务(数字、文字、条形目标)零代价, 对这类任务
     “拉氏量一致 CNN”是可落地架构。

3. **R3 — 用偶/奇能量分解作为诊断与“语义旋钮”。**
   把每层滤波器分解 `k = kₛ + kₐ`: 偶部≈势能/平滑骨架(自伴), 奇部≈方向/陀螺
   (反自伴, ⟨u,Au⟩=0)。首层该占比波动大 ⇒ 不强求; 深层总占比若显著偏离 40–60%,
   值得检查是数据偏置还是初始化问题。可视化时奇部占比越高的层 ≈ 越“方向差分型”。

4. **R4 — 方向直方图体检: 保证朝向覆盖与多样性。**
   每层保持“方向型滤波器占多数 + 各 15° 桶非空”; R(双倍角环形均值长度)接近 0
   为佳。数据本身方向有偏(如手写斜体)时 R 会升高, 属正常; 训练不足时也可能异常高。

5. **R5 — 把“可导出的拉氏量/能量”当设计目标时, 测试协议必须与基线同预算。**
   精度比较要用相同数据子集、轮数、优化器与种子(本项目 A 组同 seed=7 才有
   “+0.06/−0.07 pp”这种可比结论; 跨种子抖动 ~1.4 pp 远大于约束效应, 不做同 seed
   对照会把“约束免费”误判成“约束有害/有益”)。

---

## 复场(虚数)路线 — 实测与含义

把对话里“复数 vs 耗散”的另一支也试了一遍: 对任意实核 k, 在复 L² 场上
A = Aₛ + Aₐ(自伴+反自伴), 用复数核 **ĉ = kₛ − i kₐ** 构造 Hermitian 算子,
其实符号 = **Re ĥ + Im ĥ ∈ ℝ**(另一种取法 ĉ=kₛ+ikₐ → Re ĥ − Im ĥ)。

**在该视角下对训练的 CNN(98% 模型)实测:**

| 量(复化视角) | Conv1(8 算子) | Conv2 分量(128) |
|---|---|---|
| 动能(imag)功率占比均值 | 47.6%(21–71%) | 46.1% |
| 复化实谱范围(Re+Im) | [−5.02, +4.80] | — |
| Hermitian 正定(谱≥0) | 0/8 | 0/128 |

恒等式: **动能占比 ≡ 实框的奇部能量占比**(Parseval), 所以两种视角数字互通。

**含义**
1. 复场下“对应实值(二次)拉氏量”**自动恒成立**(Hermitian ⇒ S=½∫ĥ_c|û|² 实值),
   奇偶约束消失——奇部从"陀螺/需辛结构"变成谱实值的**动能/动量/电流项**
   (类 1D 情形 k=∂ → ĉ∝−i∂ → 实谱 ±ξ)。
2. 但 **Hermitian 正定仍是 0/136**: 动能+势能混合谱照旧变号, 复化只改“可解释语义”
   (相位、守恒流、能量视图), 不把特征层变成凸能量 —— 想正定仍须显式符号设计。
3. 纯精度/几何任务上实框架足够且更省(权重减半、无相位非线性); 复场在需要
   **相位信息或物理一致演化**(波动/量子/时频/光学)时才产生新增价值。
   这也印证对话中的取向: “复数比较理想, 但更倾向(实数)耗散”在 CNN 特征层上
   两者给出一致结论(无自由损失、均非正定), 选实框架是性价比合理的工程决定。

---

## 端到端“分类机制”如何实现 —— 定量解剖(98% 模型, 8000 测试样本)

`backend/mech.py`(推理期测量, `data/mech.json`, 页面 ⑤)

**M1 · 分类的“主战场”在卷积栈, 不在全连接**
线性读头(岭回归, 分半)可分度: 原始像素 **80.8%** → Conv 栈输出 pool2 **98.0%**
(+17.2 pp, 卷积层完成了绝大部分判别) → FC1(120 维,ReLU) **98.8%**(+0.8 pp)。
质心最近类: 60.4% → 75.7% → 86.1%。分类间隔 mean=7.7 / p10=3.4, 负间隔 0。
⇒ “机制” ≈ 平移等变算子把笔迹压成线性可分的测度; 分类器只是取 10 方向 argmax,
FC 做的是精修(去相关+拉大间隔), 不是判别的主力。

**M2 · 证据如何投给数字: 共享“墨量”通道 + 少数判别通道**
Conv1 的 ch4 对所有数字都有高激活(F=0.6, “总笔画能量”类共享通道), 判别力集中在
少数通道(ch2 F=1.3 主投 5、ch6 F=1.2 等); Conv2 出现更专一的通道
(ch12 F=1.9 主投 0/2/3/5/6/7/8/9、ch6 F=1.85 主投 1 与 4、ch5…)。
机制 = 共享基 + 少量选择性响应, 靠末层**做差**(减法读头)而非单通道专属投票。

**M3 · 末层的真正作用: 给“重叠的类云”做去相关**
类中心(120 维特征)余弦可高达 0.73–0.80(4↔9、7↔9、2↔8、3↔5), 但末层 10 个
决策方向(权重向量)之间最大余弦只有 ~0.22 —— 读头学到了近似正交的方向, 把 86%
“质心可分”抬到 98.8%。最易混淆 5↔6(17 例)、0↔6(11)、4↔9(9), 与类云重叠吻合。

**M4 · 非线性在哪一步真正生效(稀疏门控)**
Conv1/Conv2 的 ReLU 激活率 ≈ 100%(几乎不裁剪)⇒ 卷积层近似**线性算子库**;
非线性选择主要发生在 FC1: ReLU 激活率仅 ~35%, 每个样本平均关掉 65% 隐藏单元
⇒ “机制”: 线性算子测量 → 池化鲁棒聚合 → FC1 稀疏门 → 正交方向读头。
(注: 激活率随训练/bias 会变, 属该模型实测值, 非网络本质。)

---

## 局限与推广边界(诚实声明)

- 证据域: MNIST 手写数字(条带/笔画类结构), 15k×2 ≈97% 配置; 数字结论在该任务族内成立。
- 自然图像(阶跃边缘、纹理、真实照片)上奇部/方向性很可能更重要 —— 请用 R1 的
  A/B 协议在每个新任务上重测, 不要外推“偶核约束永远免费”。
- “偶核=拉氏量可导出”对应**线性二次**意义; 有 ReLU/池化时网络整体不是线性算子,
  本文约束语义作用于卷积滤波器的算子层面, 不等价于整网能量泛函存在性。
